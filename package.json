{
  "name": "@seneca/provider",
  "version": "4.0.0",
  "main": "dist/provider.js",
  "type": "commonjs",
  "types": "dist/provider.d.ts",
  "description": "Shared utilities for Seneca Provider plugins.",
  "homepage": "https://github.com/senecajs/seneca-provider",
  "keywords": [
    "seneca",
    "provider"
  ],
  "author": "Richard Rodger - richardrodger.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/senecajs/seneca-provider.git"
  },
  "scripts": {
    "test": "node --enable-source-maps --test test/**/*.test.js",
    "test-some": "node --enable-source-maps --test-name-pattern=\"$TEST_PATTERN\" --test \"test/**/*.test.js\"",
    "test-watch": "node --test --watch test/**/*.test.js",
    "test-coverage": "node --enable-source-maps --test --experimental-test-coverage test/**/*.test.js",
    "watch": "tsc --build src test -w",
    "build": "tsc --build src test",
    "doc": "seneca-doc",
    "clean": "rm -rf node_modules dist dist-test .tsbuildinfo yarn.lock package-lock.json",
    "build-test": "tsc -p tsconfig.test.json && cp test/env.json dist-test/test/",
    "watch-test": "tsc -w -p tsconfig.test.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "reset-yarn": "yarn run clean && yarn install && yarn run build && yarn test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run doc && npm run repo-tag && npm publish --access public --registry https://registry.npmjs.org "
  },
  "license": "MIT",
  "files": [
    "dist",
    "src/**/*.ts",
    "LICENSE"
  ],
  "devDependencies": {
    "@seneca/env": ">=0.5",
    "@seneca/maintain": "^0.1.0",
    "@types/node": "^24.13.3",
    "express": "^5.2.1",
    "node-fetch": "3",
    "seneca": ">=3",
    "seneca-doc": "^2.1.3",
    "seneca-entity": ">=28",
    "seneca-msg-test": "^4.1.0",
    "seneca-promisify": ">=3",
    "typescript": "^7.0.2"
  },
  "peerDependencies": {
    "@seneca/env": ">=0.4",
    "node-fetch": "3",
    "seneca": ">=3||>=4.0.0-rc2",
    "seneca-entity": ">=26",
    "seneca-promisify": ">=3"
  },
  "engines": {
    "node": ">=24"
  }
}
